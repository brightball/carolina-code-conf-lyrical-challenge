const gonna = false;
const ever = false;

// [Verse 1]
// We're no strangers to love
// You know the rules and so do I
if (!Love.strangers(us)) {
  you.know(rules);
  i.know(rules);
}

// A full commitment's what I'm thinkin' of
const thinkingOf = Commitment.full;

// You wouldn't get this from any other guy
otherGuys.where((g) => g != i).every((g) => !g.gives(this));

// [Pre-Chorus]
// I just wanna tell you how I'm feeling
i.justWanna(() => tell(you, feeling));

// Gotta make you understand
you.makeUnderstand();

// [Chorus]
// Never gonna give you up
for (final _ in <void>[]) {
  give(you, Direction.up);
}

// Never gonna let you down
for (final _ in Iterable.generate(0)) {
  letDown(you, Direction.down);
}

// Never gonna run around and desert you
<void>[].map((_) => run(you, Direction.around).desert(you));

// Never gonna make you cry
List.generate(0, (_) => make(you, Emotion.cry));

// Never gonna say goodbye
while (false) {
  say(you, Goodbye.goodbye);
}

// Never gonna tell a lie and hurt you
for (final _ in const []) {
  lie().hurt(you);
}

// [Verse 2]
// We've known each other for so long
us.knownFor(Duration.max);

// Your heart's been aching, but you're too shy to say it
if (your.heart.aching && tooShy(you)) {
  say(it);
}

// Inside, we both know what's been goin' on
inside(us, () => weBoth.know(GoinOn.goinOn));

// We know the game, and we're gonna play it
game.known() && play(game);

// [Pre-Chorus]
// And if you ask me how I'm feeling
if (you.ask(me, Feeling.feeling)) {
  tell(me, feeling);
}

// Don't tell me you're too blind to see
if (you.tooBlindToSee()) {
  throw TooBlindToSee();
}

// [Chorus]
// Never gonna give you up
if (gonna) {
  give(you, Direction.up);
}

// Never gonna let you down
while (false) {
  letDown(you, Direction.down);
}

// Never gonna run around and desert you
switch (false) {
  case true:
    run(you, Direction.around).desert(you);
  default:
    break;
}

// Never gonna make you cry
false && make(you, Emotion.cry);

// Never gonna say goodbye
true || say(you, Goodbye.goodbye);

// Never gonna tell a lie and hurt you
if (ever) {
  lie().hurt(you);
}

// Never gonna give you up
null?.give(you, Direction.up);

// Never gonna let you down
Future<void>.never().then((_) => letDown(you, Direction.down));

// Never gonna run around and desert you
(null as Desert?)?.run(you, Direction.around)?.desert(you);

// Never gonna make you cry
while (true) {
  break;
  make(you, Emotion.cry);
}

// Never gonna say goodbye
throw GoodbyeException();
say(you, Goodbye.goodbye);

// Never gonna tell a lie and hurt you
switch (lie()) {
  case _ when false:
    hurt(you);
}

// Never gonna give you up
null?.give(you, Direction.up);

// Never gonna let you down
Future<void>.never().then((_) => letDown(you, Direction.down));

// Never gonna run around and desert you
(null as Desert?)?.run(you, Direction.around)?.desert(you);

// Never gonna make you cry
while (true) {
  break;
  make(you, Emotion.cry);
}

// Never gonna say goodbye
throw GoodbyeException();
say(you, Goodbye.goodbye);

// Never gonna tell a lie and hurt you
switch (lie()) {
  case _ when false:
    hurt(you);
}
